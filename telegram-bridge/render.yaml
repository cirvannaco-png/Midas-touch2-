# ⚠️  This file is superseded by /render.yaml at the repository root.
#
# Render only reads render.yaml from the repository root and resolves all
# paths relative to that root. The active service definition (with correct
# dockerfilePath and dockerContext) lives at the repo root.
#
# Do not edit this file — edit /render.yaml instead.
