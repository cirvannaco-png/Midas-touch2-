# CI is intentionally paused during the institutional hardening pass.
# Re-enable the existing validation/test jobs only after the repository is
# internally complete and the current error backlog has been corrected.
workflow:
  rules:
    - when: never
